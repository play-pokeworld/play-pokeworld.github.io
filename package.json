{
  "name": "pokeworld-refactor",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "validate": "node tools/validate-project.mjs",
    "test": "node --test tests",
    "check": "npm run validate && npm run build && npm test",
    "download:assets": "python3 tools/download_assets.py",
    "audit": "python3 tools/audit_project.py"
  },
  "devDependencies": {
    "eslint": "^10.7.0",
    "prettier": "^3.9.5",
    "vite": "latest"
  }
}

